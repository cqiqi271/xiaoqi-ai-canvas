# 如何制作更新包

1. 在当前版本基础上更新代码。
2. 确认 `data/`、`assets/`、`output/` 和 `API/.env` 仍然保留，不要打进更新覆盖逻辑里。
3. 把整个新版目录压成 zip。
4. 保证 zip 解压后根目录里有 `update.bat` 和 `run.bat`。
5. 发给对方后，对方先解压，再双击 `update.bat`，选择旧版项目目录即可。

如果你以后要，我也可以继续给你做一个“发布更新包”的批处理，自动把当前目录打成带版本号的 zip。
